# Quiz 1

## Question 1 (1 point)

Which of the following things is a prolog variable?

**Options:**

- None of these answers
- `a`
- `'Jack'`
- `"Jack"`
- All of these answers

---

## Question 2 (1 point)

Which of the following things is a prolog atom, or constant?

**Options:**

- None of these answers
- `jack`
- All of these answers
- `\_jack`
- `Jack`

---

## Question 3 (1 point)

Which of the following things is a prolog atom, or constant?

**Options:**

- `Var`
- All of these answers
- None of these answers
- `A`
- `var(a)`

---

## Question 4 (1 point)

Which of the following things is syntactically correct prolog?

**Options:**

- `+(1,2)`
- `that(X,4)`
- All of these answers
- None of these answers
- `this(3,4)`

---

## Question 5 (1 point)

Which of the following things is a prolog structure?

**Options:**

- None of these answers
- `triangle(A,B,C)`
- `3`
- All of these answers
- `a`

---

## Question 6 (1 point)

Which of the following is a correct prolog clause that would mean "c is true if a and b are true"?

**Options:**

- `a,b:-c.`
- `a:-b,c`
- `:-c,a,b.`
- None of these answers
- All of these answers

---

## Question 7 (1 point)

Which of the following statements is true about prolog variables?

**Options:**

- None of these answers
- The scope of a prolog variable is the set of clauses in the prolog program
- All of these answers
- The scope of a prolog variable is a single clause ending in a period
- The scope of a prolog variable depends on how the variable is declared by the prolog programmer

---

## Question 8 (1 point)

What does "scope" of a prolog variable mean?

**Options:**

- It means that all instances of a variable with the same name occurring in the same scope are the same value, and the scope of a non-anonymous variable is the whole set of prolog clauses in which it appears
- None of these answers
- It means that all instances of a non-anonymous variable with the same name occurring in the same scope are the same value, and the scope of a non-anonymous variable is the single prolog clause in which it appears
- All of these answers
- The scope of a variable governs the values that a prolog variable can take, for example, numbers versus strings.

---

## Question 9 (1 point)

Which of the following statements is true?

**Options:**

- Declarative knowledge expressed as prolog clauses never has a procedural interpretation
- Declarative knowledge expressed as prolog clauses always has a procedural interpretation that does not depend on the ordering of the clauses
- None of these answers
- All of these answers
- Declarative knowledge expressed as prolog clauses does not depend on the ordering of the clauses, but in general the procedural interpretation of prolog clauses does depend on the ordering of the clauses

---

## Question 10 (1 point)

Which of the following statements is true?

**Options:**

- Prolog programs assume that a query will be issued to begin program execution, similarly to the way a Java program assumes its main method will be invoked to begin program execution
- All of these answers
- In order to implement iteration with prolog, the programmer needs to implement recursion, because prolog does not have a loop construct
- None of these answers
- Prolog programs rely on a prolog interpreter in order to run, analogously to how Java programs rely on a Java Virtual Machine in order to run
